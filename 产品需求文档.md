# 多平台代码仓库聚合分析工具 GitUnite — 产品需求文档

| 项目 | 内容 |
| --- | --- |
| 版本 | v1.4 |
| 更新日期 | 2026-08-05 |
| 进度 | M1 基础骨架已完成（详见第七章） |
| 目标用户 | 开发者本人（单用户） |
| 运行方式 | 纯前端应用，在本地浏览器中运行，不部署、不需要后端服务器 |
| 数据存储 | 浏览器本地 IndexedDB（业务数据）+ LocalStorage（Token/配置），数据不上传到任何第三方 |
| 开源协议 | MIT |

---

## 一、产品概述

GitUnite 是一个本地运行的开发者个人数据看板。用户通过授权接入自己的 **GitHub** 和 **Gitee** 账户后，应用在浏览器内调用两家平台的公开 API，聚合名下及参与贡献的所有仓库（含私有仓库，以 Token 权限为边界），从提交行为、代码量、活跃时间、语言分布、协作关系、PR/Issue 等维度生成可视化的"编码档案"，并支持年度报告、成就徽章、数据导出与分享卡片。

应用本身不收集任何数据，所有 Token、仓库信息、提交记录只保存在当前浏览器中。

### 1.1 设计目标

- **隐私优先**：无后端、无数据上报，Token 不出浏览器。
- **完整统计**：默认包含私有仓库，所有数据只存在本地，不影响统计完整性。
- **双平台对等**：GitHub 与 Gitee 各只绑定一个账号，数据抽象为统一接口，看板支持「单平台 / 聚合」无缝切换。
- **增量同步**：第二次打开只拉取上次之后的新数据，避免重复请求和触发限流。

### 1.2 非目标（v1 不做）

- 不做团队/多用户协作版；
- 不做代码内容分析（不读源码、不做缺陷扫描）；
- 不做 Git 服务器端集成（无 Webhook、无实时推送）；
- 不部署上线，仅本地运行；
- 移动端只做适配浏览，不保证复杂图表交互体验。

---

## 二、统一数据模型

为消除 GitHub / Gitee 的 API 差异，应用内部统一为以下结构，两个平台的适配器都输出该结构：

```text
Platform         = 'github' | 'gitee'
UnifiedUser      { id, login, name, avatarUrl, platform, htmlUrl }
UnifiedRepo      { id, platform, owner, name, fullName, description,
                   language, languages: Record<string, number>,
                   stargazersCount, forksCount, isPrivate, isFork,
                   isOwned, isContributed, updatedAt, htmlUrl }
UnifiedCommit    { id, repoId, platform, sha, message, authorLogin,
                   authorName, authoredAt(UTC), additions, deletions,
                   filesChanged, htmlUrl }
UnifiedIssue     { id, repoId, platform, number, type: 'pr'|'issue',
                   state, title, createdAt, closedAt, mergedAt, htmlUrl }
SyncCursor       { platform, repoId, lastCommitSha, lastSyncedAt, etag }
```

**"参与贡献的仓库"定义（并集口径）：**

1. Token 授权下 `/user/repos` 返回的全部仓库（自有、协作者、组织成员）；
2. 通过 Search API 发现的、当前用户提交过被合并 PR 的仓库；
3. 用户可在设置中手动隐藏不希望展示的仓库。

仓库用标签区分：`自有`、`组织`、`Fork`、`PR 贡献`。

---

## 三、功能需求

### 3.1 多平台账户接入

#### 授权方式

纯前端应用不持有 client_secret，无法使用传统 OAuth 授权码流程。v1 统一采用 **Personal Access Token（PAT）手动录入**：

- **GitHub**：引导用户到 `Settings → Developer settings → Personal access tokens` 创建 Fine-grained token，勾选 **Contents: Read-only**、**Metadata: Read-only**；需要 PR/Issue 统计时再加 **Pull Requests: Read**、**Issues: Read**。
- **Gitee**：引导用户到 `设置 → 私人令牌` 生成，勾选 `projects`、`user_info`、`pull_requests`、`issues`（均只读）。

用户在应用内粘贴 Token，应用调用 `/user` 接口验证有效性后保存。

#### Token 存储

- Token 只保存在浏览器 LocalStorage，键名带版本前缀；
- 提供"退出登录 / 清除 Token"按钮，清除时连同 IndexedDB 中该平台的全部缓存数据一起删除；
- Token 只通过 HTTP 头部 `Authorization` 携带，不拼进 URL、不写入任何日志；
- 页面显著提示："任何浏览器扩展都可能读取本机存储，请勿在不可信设备上使用。"

#### 状态显示

每个平台一张账户卡片：头像、昵称、登录状态、Token 权限范围、最近同步时间、剩余 API 配额（GitHub 读 `x-ratelimit-remaining`，Gitee 读 `rate-limit-remaining`）。异常态需区分：Token 无效、网络错误、超出速率限制（显示重置时间）、权限不足（指出缺哪个 scope）。

---

### 3.2 仓库聚合与筛选

- 三个 Tab：`全部` / `GitHub` / `Gitee`；
- 列表字段：仓库名、平台图标、描述、主语言、Star 数、Fork 数、最后一次提交时间、贡献类型标签；
- 支持：关键字搜索（仓库名/描述）、按语言筛选、排序（最近活跃、Star 数、名称、提交数）、仅看"我贡献过代码"的开关；
- 仓库数 > 100 时使用虚拟滚动。

---

### 3.3 数据分析看板

所有统计卡片支持 `GitHub / Gitee / 聚合` 视角切换。所有展示层时间统一转换为 **用户本地时区**（设置中可手动覆盖，默认浏览器时区）。

#### 3.3.1 基础统计

| 指标 | 口径 |
| --- | --- |
| 仓库总数 | 去除已隐藏仓库；Fork 单独成项，可选是否计入 |
| 总提交次数 | 当前用户作为 author 的 commit 数，按 SHA 去重 |
| 代码新增行数 additions | GitHub 优先用 `stats/contributors` 周聚合接口；Gitee 无对应接口时逐 commit `stats.additions` 累加 |
| 代码删除行数 deletions | 同上 |
| 平均每次提交变更行数 | `(additions + deletions) / commits`，零提交时显示 0 |

#### 3.3.2 活跃度分析

- 首次提交日期、最近提交日期、活跃总天数（有提交的不同日期数）；
- 日均提交数 = 总提交数 / 活跃总天数（不除以注册时长，避免虚低）；
- 最长连续提交天数（streak）及起止日期，同时显示当前连续天数；
- **活跃时段分布**：按本地时区 0–23 点分桶，柱状图展示，标注提交最多的连续 3 小时为"编码黄金时段"；
- **星期分布**：周一至周日提交强度；
- **深夜提交占比**：本地时间 0:00–6:00 的提交占比。

#### 3.3.3 语言与技术栈

- **语言占比环形图**：用仓库 `languages` 接口返回的字节数加权（不只取主语言），支持按平台/聚合切换；
- **Top N 语言列表**：语言名、字节数、占比、对应仓库数；
- **技术栈趋势**：按年/季度展示该时段各语言的字节数或提交数（堆叠面积图），当年出现、历史未出现的语言标记为"新学"。

---

### 3.4 提交记录时间轴

- 日历热力图形式（详见 3.6），点击某天弹出当日提交列表；
- 单日详情字段：仓库名、commit message（首行加粗、正文可展开）、增删行数、提交时间（精确到分）、跳转原平台链接；
- commit message 全文本地索引，支持关键字搜索、回车跳转、高亮匹配；
- 提供"仅看 merge commit / 仅看非 merge commit"过滤。

---

### 3.5 协作网络分析

- **节点**：
  - 中心：当前用户（头像节点）；
  - 一级节点：用户有过提交的仓库，节点大小按该仓库中用户的提交数归一化；
  - 二级节点（可选开关）：在同一仓库中与用户共同提交过的其他作者，节点大小按共同提交次数归一化。
- **边**：用户—仓库（粗度=提交次数）、用户—协作者（粗度=共同仓库数）。
- **布局**：力导向图，支持拖拽、缩放、框选。
- **交互**：
  - Hover 显示 tooltip（仓库：语言/Star/我的提交数；协作者：共同仓库列表与共同提交数）；
  - 单击仓库节点：高亮该仓库关联的所有提交与协作者，可一键跳转到该仓库详情；
  - 双击协作者：在侧边抽屉列出该协作者与"我"的共同提交记录。
- 数据来源：遍历已缓存 commit 的 `authorLogin` / `authorName` 按 `(repoId, author)` 聚合，不额外发起 API 请求。

---

### 3.6 热力图

- 两种热力图：
  1. **提交频次热力图**：仿 GitHub 贡献日历，颜色深浅 = 当日提交数；
  2. **代码量热力图**：颜色深浅 = 当日 additions + deletions。
- 平台切换：GitHub / Gitee / 聚合；
- 时间范围快捷切换：近 1 年 / 近 2 年 / 全部（Gitee API 翻页有上限，全量时若被截断需明确提示）；
- 单元格 Hover 显示日期、提交数、增删行数；点击进入 3.4 的单日详情；
- 提供配色切换：默认 / 色弱友好 / 深色。

---

### 3.7 Pull Request & Issue 贡献

- 我创建的 PR 数、被合并数、Closed 未合并数；Issue 同理；
- **平均合并耗时**：对已合并 PR，计算 `mergedAt - createdAt`，输出中位数与平均值（用中位数避免极端值）；
- 参与仓库的贡献度排名：按 `PR 数 + Issue 数` 排序，显示 Top 10；
- 若 Token 未授权 PR/Issue 读权限，在该模块显示引导说明而非报错。

---

### 3.8 年度报告 & 成就徽章

#### 年度编程年鉴

- 年份选择器（默认当前年，可回溯任意已同步年份）；
- 内容：
  - 年度提交数、代码新增/删除行数、活跃天数、活跃天数占比；
  - 最长连续提交天数及起止日期；
  - 最活跃月份、最活跃星期、最活跃时段；
  - 深夜提交占比、周末提交占比；
  - 今年新使用的语言、使用最多的 Top 5 语言；
  - 贡献过的仓库数、今年首次贡献的新仓库数；
  - commit message 词云（去除 merge / fix / update 等停用词）。

#### 成就徽章（本地虚拟奖励）

| 徽章 | 获得条件 |
| --- | --- |
| 夜猫子 | 本地时间 0–6 点提交 ≥ 100 次 |
| 早起鸟 | 本地时间 5–8 点提交 ≥ 100 次 |
| 全栈探索者 | 累计使用 ≥ 5 种语言 |
| 语言大师 | 累计使用 ≥ 10 种语言 |
| 开源贡献者 | PR 贡献型仓库数 ≥ 3 |
| 码力全开 | 单日提交 ≥ 20 次 |
| 连续提交王 | 连续 30 天有提交 |
| 百日坚持 | 连续 100 天有提交 |
| 千提交 | 累计提交 ≥ 1000 |
| 万行代码 | 累计新增 ≥ 10000 行 |

徽章只在本地计算并存于 IndexedDB，可在分享卡片中选择是否露出。

---

### 3.9 数据导出与分享

- **JSON 导出**：导出当前筛选范围内的全部 `UnifiedRepo / UnifiedCommit / UnifiedIssue` 与聚合统计，文件名 `gitunite-export-YYYYMMDD.json`；
- **CSV 导出**：
  - `commits.csv`：sha, platform, repo, message, date(local), additions, deletions；
  - `repos.csv`：平台、全名、语言、Star、是否私有、我的提交数；
  - 使用 UTF-8 BOM，保证 Excel 直接打开中文不乱码；
- **分享卡片（PNG）**：
  - 1200×630，包含头像、昵称、总提交数、使用语言数、活跃天数、最长 streak、Top 3 语言、当年徽章；
  - 生成界面显著提示："卡片将包含你的头像和昵称，请勿在不希望暴露身份的场合分享"，并提供"隐藏头像/昵称"开关；
  - 通过 `html-to-image` 在前端绘制；头像需缓存为 DataURL 以避免 canvas 跨域污染；
  - 提供"复制到剪贴板"与"下载 PNG"两个按钮。

---

## 四、数据同步策略

### 4.1 首次同步

1. 校验 Token → 拉取用户信息；
2. 拉取仓库列表（分页，每页 100，写入 IndexedDB），默认包含公有与私有仓库；
3. **Gitee 首次同步前弹窗提示**："Gitee 平台无代码行聚合接口，开启代码明细同步会逐提交请求详情，耗时较长且易触发限流。可随时在设置中关闭。"用户确认后开始；
4. 对每个仓库并发拉取：
   - **GitHub**：先调 `GET /repos/{owner}/{repo}/stats/contributors`（返回 202 时指数退避重试，最多 3 次）拿到按周聚合的 additions/deletions；再调 `GET /repos/{owner}/{repo}/commits?author=<user>` 翻页拉取提交明细，直到无下一页或达 API 上限；
   - **Gitee**：调 `GET /repos/{owner}/{repo}/commits?author=<user>` 翻页；代码明细开关开启时，再对每个提交调详情接口取 additions/deletions，关闭时只记录提交次数与 message，不记录代码行数；
5. 显示全局进度（`已完成仓库 x / y`）与预计剩余时间，Gitee 代码明细同步时额外显示"代码行明细同步中，预计较慢"提示；
6. 允许中途关闭页面，下次回来通过 `SyncCursor` 续传。

### 4.2 增量同步

- 每个仓库记录 `lastCommitSha` 与 `lastSyncedAt`；刷新时只请求 `?since=<lastSyncedAt>` 拉新提交；
- 用 `If-None-Match` / `ETag` 对仓库列表、语言接口做条件请求，命中 304 不消耗配额；
- 默认每次打开自动做轻量增量（只拉最近 30 天），全量同步由用户手动触发。

### 4.3 速率限制

- 响应拦截器读剩余配额，低于阈值（GitHub 剩余 200 点）暂停非关键请求并提示；
- 被限流（403/429）时读 `Retry-After` 或 `x-ratelimit-reset`，到点自动恢复；
- 同一仓库请求队列串行 + 全局并发上限（GitHub ≤ 4，Gitee ≤ 2）；
- **代码明细同步开关**（设置中提供，默认开启）：
  - 开启时：GitHub / Gitee 均拉取每次提交的 additions/deletions，统计完整但 Gitee 较慢；
  - 关闭时：只同步提交次数、message、时间，不拉取代码行数，代码量相关图表显示为"未开启代码明细统计"；
  - 关闭后可随时重新开启，重新开启后触发对缺失数据的补全同步；
  - Gitee 首次同步前弹窗告知用户该开关的影响（见 4.1）。

---

## 五、非功能需求

- **性能**：已有缓存时首屏渲染 < 1s；50 仓库 / 5000 提交规模下，看板筛选切换 < 300ms；热力图、协作图在 1 万节点以下保持流畅，超限降采样并提示；
- **浏览器兼容**：本地开发面向最新版 Chrome / Edge（推荐），Firefox、Safari 最近两个大版本可用；
- **可靠性**：所有网络请求 15s 超时、最多重试 3 次（指数退避）；IndexedDB 写入用事务，schema 带版本号支持迁移；删除 Token、清空数据等危险操作二次确认；
- **可访问性**：图表提供"纯文本/表格视图"切换；所有交互可键盘操作；
- **国际化**：v1 中文简体，代码中文案走 i18n key，为后续开源留好英文扩展位。

---

## 六、技术栈

> 选择原则：Vue 3 生态成熟、ECharts 覆盖全部图表、全量 TypeScript、零运行时后端依赖，适合在本地直接 `pnpm dev` 运行。

### 6.1 核心框架

| 分类 | 选型 | 说明 |
| --- | --- | --- |
| 语言 | TypeScript ^5.5 | 全量 TS，开启 strict |
| 框架 | Vue ^3.4 | `<script setup>` + Composition API |
| 构建 | Vite ^5.4 | 本地开发冷启动快 |
| 路由 | Vue Router ^4.4 | 视图切换 |
| 状态管理 | Pinia ^2.2 | 模块化 store |
| 工具集 | VueUse ^11 | `useStorage`、`useWebWorker` 等 |

### 6.2 UI 与样式

| 分类 | 选型 | 说明 |
| --- | --- | --- |
| 原子 CSS | Tailwind CSS ^3.4 | 快速搭界面 |
| 组件库 | Naive UI ^2.38 | Vue 3 原生、TS 友好、暗色主题开箱即用 |
| 图标 | lucide-vue-next | 统一风格、按需引入 |
| Markdown（可选） | markdown-it + DOMPurify | 若渲染仓库 README；commit message 不渲染 HTML |

### 6.3 数据可视化

| 分类 | 选型 | 说明 |
| --- | --- | --- |
| 图表主力 | Apache ECharts ^5.5 | 日历热力图、环形图、柱状/折线/堆叠面积、雷达 |
| Vue 封装 | vue-echarts ^7 | 按需引入减小体积 |
| 协作网络图 | ECharts `graph` 系列 | 力导向布局内置即可 |
| 词云 | echarts-wordcloud | 年度报告词云 |
| PNG 导出 | html-to-image ^1.11 | 生成分享卡片 |

### 6.4 数据层

| 分类 | 选型 | 说明 |
| --- | --- | --- |
| 本地数据库 | Dexie.js ^4.0 | IndexedDB 封装，链式查询，TS 友好，schema 版本迁移 |
| HTTP 客户端 | axios ^1.7 | 拦截器统一处理 Token、限流、ETag、重试 |
| CSV 导出 | Papa Parse ^5.4 | 支持 UTF-8 BOM |
| 时间处理 | dayjs ^1.11 | 体积小，支持 `weekOfYear`、`utc`、`timezone` 插件 |
| 工具函数 | es-toolkit | 按需打包 |

### 6.5 工程化

| 分类 | 选型 |
| --- | --- |
| 代码规范 | ESLint ^9 + `@antfu/eslint-config` |
| 格式化 | Prettier ^3 |
| Git Hooks | simple-git-hooks + lint-staged |
| 提交规范 | Conventional Commits（commitlint） |
| 单元测试 | Vitest ^2 + @vue/test-utils |
| 包管理 | pnpm |

### 6.6 推荐目录结构

```text
GitUnite/
├─ public/
├─ src/
│  ├─ api/
│  │  ├─ github/              # GitHub 适配器
│  │  │  ├─ client.ts         # axios 实例 + 拦截器
│  │  │  ├─ repos.ts
│  │  │  ├─ commits.ts
│  │  │  └─ stats.ts
│  │  ├─ gitee/               # Gitee 适配器（结构同上）
│  │  ├─ types.ts             # UnifiedXxx 类型定义
│  │  └─ index.ts             # 平台适配器注册表
│  ├─ db/
│  │  ├─ schema.ts            # Dexie 表结构与版本
│  │  └─ repositories/        # repoRepo、commitRepo、cursorRepo
│  ├─ sync/
│  │  ├─ engine.ts            # 同步引擎（队列、并发、限流、重试）
│  │  ├─ github-strategy.ts
│  │  └─ gitee-strategy.ts
│  ├─ stores/                 # Pinia: auth、repos、commits、analytics、ui
│  ├─ composables/
│  │  ├─ useSync.ts
│  │  ├─ useAnalytics.ts      # 聚合计算（可放 Web Worker）
│  │  ├─ useHeatmap.ts
│  │  └─ useCollaborationGraph.ts
│  ├─ workers/
│  │  └─ analytics.worker.ts  # 大数据量聚合放 Worker 避免阻塞主线程
│  ├─ components/
│  │  ├─ common/
│  │  ├─ charts/              # Heatmap、Donut、Bar、ForceGraph
│  │  ├─ repo/
│  │  ├─ commit/
│  │  └─ share/
│  ├─ views/                  # Dashboard、Repos、Timeline、Collaboration、Yearbook、Settings
│  ├─ utils/
│  ├─ styles/                 # Tailwind 入口、主题变量
│  ├─ i18n/                   # zh-CN、en-US
│  ├─ router/
│  ├─ App.vue
│  └─ main.ts
├─ tests/
├─ index.html
├─ vite.config.ts
├─ tailwind.config.ts
├─ tsconfig.json
└─ package.json
```

### 6.7 关键依赖清单

```jsonc
{
  "dependencies": {
    "vue": "^3.4",
    "vue-router": "^4.4",
    "pinia": "^2.2",
    "@vueuse/core": "^11.0",
    "naive-ui": "^2.38",
    "lucide-vue-next": "latest",
    "echarts": "^5.5",
    "vue-echarts": "^7.0",
    "echarts-wordcloud": "^2.1",
    "dexie": "^4.0",
    "axios": "^1.7",
    "dayjs": "^1.11",
    "papaparse": "^5.4",
    "html-to-image": "^1.11",
    "es-toolkit": "latest"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "vite": "^5.4",
    "@vitejs/plugin-vue": "latest",
    "tailwindcss": "^3.4",
    "postcss": "latest",
    "autoprefixer": "latest",
    "eslint": "^9.0",
    "@antfu/eslint-config": "latest",
    "prettier": "^3.3",
    "vitest": "^2.0",
    "@vue/test-utils": "latest",
    "simple-git-hooks": "latest",
    "lint-staged": "latest"
  }
}
```

---

## 七、分期交付建议

| 里程碑 | 范围 | 产出 |
| --- | --- | --- |
| **M1 基础骨架** ✅ | ~~项目脚手架~~、~~路由~~、~~Pinia~~、~~Dexie~~、~~Tailwind~~、~~Naive UI~~、~~PAT 登录页 UI~~、~~GitHub `/user` 校验适配器~~；剩余：AccountCards 与 auth store 接线、Gitee `/user` 校验、剩余配额展示 | 能登录、看到头像与剩余配额（接线中） |
| **M2 仓库与提交同步** | GitHub/Gitee 适配器、仓库列表、同步引擎（并发/限流/断点续传）、IndexedDB 缓存 | 能拉到仓库并本地浏览 |
| **M3 核心看板** | 基础统计、活跃度、语言分布、热力图、提交时间轴 | 主看板可用 |
| **M4 协作网络 & 年度报告** | 力导向图、年鉴页、徽章系统、词云 | 趣味性功能完整 |
| **M5 PR/Issue & 导出分享** | PR/Issue 统计、JSON/CSV 导出、PNG 分享卡片 | 可对外分享 |
| **M6 打磨开源** | i18n 英文、暗色/色弱主题、单元测试、README、贡献指南、MIT 协议 | v1.0 发布 |

---

## 八、已确认决策

1. **私有仓库**：默认包含在统计范围内，不提供排除开关。所有数据只存在本地浏览器，无隐私泄露风险。
2. **账号数量**：GitHub、Gitee 每平台仅绑定一个账号，不支持同时登录多个账号。
3. **Gitee 代码明细同步**：默认开启；首次同步前弹窗提示用户该操作耗时较长、易触发限流，用户确认后继续；设置中可随时关闭，关闭后代码量图表显示为"未开启代码明细统计"，重新开启则补全缺失数据。
4. **分享卡片**：包含头像与昵称，在分享卡片生成界面显著提示"卡片将包含你的头像和昵称，请勿在不希望暴露身份的场合分享"。
